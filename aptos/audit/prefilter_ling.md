# ling  — inclusionai/ling-2.6-flash:free

- **Severity**: CRITICAL, Location: `sources/ONE.move:open_impl`, Issue: `price_8dec()` can abort with `E_STALE` or `E_PRICE_ZERO` if Pyth feed is stale, deregistered, or returns zero; caller must ensure fresh VAA update, but no guarantee — impacts all oracle-dependent entrypoints. Impact: Transactions revert, locking funds or preventing arbitrage. Recommendation: Require callers to pass a validated `price: u128` parameter or enforce a `try_price` pattern with revert-on-staleness; add a grace period or circuit-breaker flag. Confidence: HIGH.
- **Severity**: HIGH, Location: `sources/ONE.move:liquidate`, Issue: `new_p` computed as `product_factor * (total_sp - debt) / total_sp` can underflow/round-down to zero if `total_sp == debt` (edge when SP empty or single trove equals total_sp), violating `MIN_P_THRESHOLD` and stalling future liquidations. Impact: Protocol halts on critical path; funds stuck. Recommendation: Guard with `assert!(total_sp > debt)` and ensure `product_factor` scaling preserves non-zero floor; add epsilon or revert with clearer error. Confidence: HIGH.
- **Severity**: HIGH, Location: `sources/ONE.move:redeem_from_reserve`, Issue: `coll_out` computed via `(net * 1e8 / price)` can exceed `u64` on extreme price magnitudes or large `net`, causing silent truncation before `assert!(coll_out <= collateral)`; also `reserve_coll` may be undercollateralized if multiple redemptions race. Impact: Invalid collateral amounts, potential loss of peg or reserve depletion. Recommendation: Use safe math (`u128` intermediates) and assert `coll_out <= u64_MAX`; cap redemption size against reserve liquidity. Confidence: HIGH.
- **Severity**: HIGH, Location: `sources/ONE.move:sp_settle`, Issue: `pending_one` and `pending_coll` computed via `(delta * balance / snap_p)` where `snap_p` can be zero (early return guards against zero but not near-zero); if `snap_p` is tiny, division can overflow u64. Impact: Arithmetic overflow/revert, loss of rewards. Recommendation: Use `u128` multiplication before division and assert `snap_p >= MIN_P_THRESHOLD`; scale rewards with `(delta * balance * PRECISION) / snap_p`. Confidence: HIGH.
- **Severity**: MEDIUM, Location: `sources/ONE.move:price_8dec`, Issue: `pow10` loop up to 38 is safe, but `abs_e >= 8` branch can produce zero if `raw < pow10(abs_e - 8)`; second `assert!(result > 0)` catches but aborts. Impact: Legitimate low-price feeds cause revert. Recommendation: Return zero price on underflow and let caller handle; or use `u128` saturation. Confidence: MEDIUM.
- **Severity: MEDIUM, Location: `sources/ONE.move:open_trove`, Issue: `open_trove` with `coll_amt = 0` allowed (debt-only top-up); no guard against zero collateral when opening new trove, enabling dust troves that complicate accounting. Impact: Edge-case inflation of total_troves with zero collateral. Recommendation: Require `coll_amt > 0` for new troves or enforce `new_collateral >= MIN_COLL`. Confidence: MEDIUM.
- **Severity: MEDIUM, Location: `sources/ONE.move:close_trove`, Issue: `close_trove` burns `t.debt` and withdraws collateral; if `primary_fungible_store::withdraw` fails (e.g., dust debt post-redeem), state may partially revert. Impact: Partial revert leaves trove in inconsistent state. Recommendation: Ensure debt >= MIN_DEBT before close; use `result` pattern or revert early. Confidence: MEDIUM.
- **Severity: LOW, Location: `sources/ONE.move:init_module`, Issue: `resource_account::retrieve_resource_account_cap` may not be idempotent; if origin changes between publish and destroy, cap may be unrecoverable. Impact: Permanent lock of package upgradeability. Recommendation: Store cap in a separate, non-upgradeable module or use deterministic derivation. Confidence: LOW.
- **Severity: LOW, Location: `sources/ONE.move:force_invariants`, Issue: No test coverage for oracle-dependent paths; unit tests stub Pyth, so integration risk is untested. Impact: Undetected regression in price handling. Recommendation: Add mock oracle tests with price staleness, zero, and extreme values. Confidence: LOW.

GREEN

---
tokens: in=14478 out=1006 dt=7.4s
